// Root layout is minimal — html/body, fonts, and locale providers live in [locale]/layout.tsx
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
